const url = 'https://give-me-app.herokuapp.com/api'
const users = '/users'

export const signIn = `${users}/signin`
export const signUp = `${users}/signup`
export const dashboard = `${users}/me/dashboard`
// enviroment
export const signOut = `${users}/signout`

export default url
